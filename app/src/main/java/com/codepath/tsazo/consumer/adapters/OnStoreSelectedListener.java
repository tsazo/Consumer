package com.codepath.tsazo.consumer.adapters;

import com.codepath.tsazo.consumer.models.Store;

//Define a interface listener
public interface OnStoreSelectedListener {
    void onStoreSelected(Store selectedStore);
}
